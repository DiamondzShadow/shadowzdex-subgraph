# Goldsky API key — write access. NEVER commit.
# Provision at https://app.goldsky.com (Settings → API keys).
GOLDSKY_API_KEY=

# Goldsky project ID — informational, also used by `goldsky` CLI when scoping.
GOLDSKY_PROJECT_ID=
